# 🔧 Лабораторная работа №2: SQL-реализация сервисных центров

Продолжение модели из ЛР1 (система записи в сервисные центры). В этом наборе скриптов есть полное DDL, наполнения, примеры DML, агрегатных запросов, JOIN'ов и представлений для PostgreSQL.

## Состав
- `schema.sql` — пересоздание типов и таблиц с ключами, CHECK и индексами.
- `seed.sql` — тестовые данные для пользователей, мастеров, устройств, слотов, тикетов и записей.
- `dml_examples.sql` — примеры вставки нового тикета с бронью слота, обновления статусов и удаления отменённых записей.
- `queries.sql` — осмысленные выборки с агрегацией, HAVING, ORDER BY и соединениями.
- `views.sql` — 3 представления: сводка по центрам, сводка по клиентам и расписание мастеров.

## Как выполнить (psql)
```bash
psql -f lab-02/schema.sql
psql -f lab-02/seed.sql
psql -f lab-02/views.sql
psql -f lab-02/dml_examples.sql    # при необходимости модификаций
psql -f lab-02/queries.sql         # посмотреть результаты запросов
```

## Что покрыто
1. **Создание схемы** — типы ENUM, таблицы, уникальности, внешние ключи, CHECK.
2. **Наполнение** — фиктивные данные с реальными связями (слоты мастеров, тикеты и записи).
3. **Простые DML** — INSERT нового обращения, UPDATE статусов, DELETE отменённых записей с освобождением слотов.
4. **Агрегация** — SUM/AVG/COUNT, HAVING и сортировка.
5. **JOIN-запросы** — детализация расписания и выборки по клиентам/устройствам.
6. **Представления** — аналитические витрины с вычисляемыми показателями.

Все скрипты используют уже спроектированную модель из ЛР1 и могут выполняться на чистой базе PostgreSQL без дополнительных расширений.
